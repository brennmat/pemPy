[PEMCELLPSU]
### TYPE = pps
### COMPORT = /dev/serial/by-id/usb-Silicon_Labs_CP2102_USB_to_UART_Bridge_Controller_0001-if00-port0
# PSU type:
TYPE = riden
# Serial port:
COMPORT = /dev/serial/by-id/usb-1a86_USB_Serial-if00-port0
# Serial port data rate (Riden only):
BAUD = 115200
# Current mode (Riden only, HIGH or LOW):
CURRENTMODE = HIGH

[LOADCELL]
# number of readings for averaging into a single value:
AVG_READINGS = 10
# GPIO pin for DOUT:
DOUT_PIN = 5
# GPIO pin for SCK:
SCK_PIN = 6

[ELECTROLYSIS]
# Target for water amount at end (grams):
WATER_TARGET = 20.0
# Number of iterations per time step (number of voltage, current and weight readings):
STEP_ITERATIONS = 10
# Time to ramp up current at start (seconds):
RAMPTIME = 1200
# Target current at start (A):
MINCURRENT = 1.0
# Target current after ramp up (A):
MAXCURRENT = 5.0
# Voltage limit (V):
MAXVOLTAGE = 7.0
